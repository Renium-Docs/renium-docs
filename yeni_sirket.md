# 🏢 Yeni Şirket Dokümantasyonu Ekleme Rehberi

Bu rehber, sisteme yeni bir şirket için özel dokümantasyon eklemenizi adım adım anlatır.
---

## 🚀 Hızlı Başlangıç

Yeni bir şirket eklemek için 5 adım:

1. Şirket klasörü oluştur
2. `merge-docs.js` güncelle
3. ✅ Sidebar oluştur
4. ✅ `docusaurus.config.ts` güncelle
5. ✅ Test et

---

## 📝 Adım Adım Kurulum

### Adım 1: Şirket Klasörü Oluşturun

Yeni şirket için klasör yapısı oluşturun:

```bash
# Örnek: "TechCorp" şirketi için
mkdir -p docs/companies/techcorp/techcorp-special

# İsteğe bağlı: Mevcut kategorilere özel dosyalar
mkdir -p docs/companies/techcorp/octo-web
mkdir -p docs/companies/techcorp/octo-desktop
mkdir -p docs/companies/techcorp/backup
```

**Klasör yapısı:**
```
docs/companies/techcorp/
├── techcorp-special/          # TechCorp'a özel yeni kategori
│   └── intro.md
├── octo-web/                  # Octo Web'e özel dosyalar (opsiyonel)
│   └── techcorp-feature.md
└── octo-desktop/              # Octo Desktop'a özel (opsiyonel)
    └── techcorp-config.md
```

### Adım 2: Örnek Doküman Oluşturun

```bash
# TechCorp özel kategori için intro dosyası
cat > docs/companies/techcorp/techcorp-special/intro.md << 'EOF'
---
title: TechCorp Özel Özellikler
sidebar_position: 1
---

# TechCorp İçin Özel Özellikler

Bu bölüm sadece TechCorp şirketi için hazırlanmıştır.

## Özellikler

- Özel Dashboard
- API Entegrasyonu
- Gelişmiş Raporlama

## Başlangıç

EOF
```

### Adım 3: merge-docs.js Dosyasını Güncelleyin

`merge-docs.js` dosyasını açın ve şirket listesine ekleyin:

```javascript
// Şirket listesi - buraya yeni şirketler ekleyebilirsiniz
const companies = [
  'renium', 
  'aws', 
  'azure',
  'techcorp'  // ← YENİ ŞİRKET EKLE
];
```

**Tam dosya yolu:** `merge-docs.js` (proje root'unda)

### Adım 4: Sidebar Dosyası Oluşturun

Yeni şirket için sidebar dosyası oluşturun:

```bash
# sidebars.techcorp.ts dosyası oluştur
cat > sidebars.techcorp.ts << 'EOF'
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docs: [
    {
      type: 'doc',
      id: 'intro',
      label: 'Giriş',
    },
    {
      type: 'category',
      label: 'Octo Desktop',
      items: [
        {
          type: 'autogenerated',
          dirName: 'octo-desktop',
        },
      ],
    },
    {
      type: 'category',
      label: 'Octo Web',
      items: [
        {
          type: 'autogenerated',
          dirName: 'octo-web',
        },
      ],
    },
    {
      type: 'category',
      label: 'Backup',
      items: [
        {
          type: 'autogenerated',
          dirName: 'backup',
        },
      ],
    },
    {
      type: 'category',
      label: 'TechCorp Özel',
      collapsed: false,
      items: [
        {
          type: 'autogenerated',
          dirName: 'techcorp-special',
        },
      ],
    },
  ],
};

export default sidebars;
EOF
```

**Önemli:** Sidebar dosyası adı `sidebars.{şirket-adı}.ts` formatında olmalı (küçük harf).

### Adım 5: docusaurus.config.ts Dosyasını Güncelleyin

`docusaurus.config.ts` dosyasını açın ve plugins bölümüne yeni şirketi ekleyin:

```typescript
plugins: [
  require.resolve('docusaurus-plugin-image-zoom'),
  
  // ... mevcut şirketler (renium, aws, azure)
  
  // 🏢 TechCorp şirketi için özel dokümanlar - YENİ
  [
    '@docusaurus/plugin-content-docs',
    {
      id: 'techcorp',
      path: 'docs/merged/techcorp',
      routeBasePath: 'techcorp/docs',
      sidebarPath: './sidebars.techcorp.ts',
      editUrl: 'https://github.com/Renium-Docs/renium-docs/tree/main/',
    },
  ],
],
```

**Önemli Noktalar:**
- `id: 'techcorp'` → Benzersiz ID (küçük harf, tire ile)
- `path: 'docs/merged/techcorp'` → Merge sonrası path
- `routeBasePath: 'techcorp/docs'` → URL path'i
- `sidebarPath: './sidebars.techcorp.ts'` → Sidebar dosya adı

### Adım 6: Test Edin

```bash
# 1. Dokümanları birleştir
npm run merge

# 2. Kontrol et
ls -la docs/merged/techcorp/

# 3. Start et
npm start

# 4. Tarayıcıda test et
# http://localhost:3000/techcorp/docs/intro
```

### Adım 7: Git'e Commit Edin

```bash
git add docs/companies/techcorp/
git add merge-docs.js
git add sidebars.techcorp.ts
git add docusaurus.config.ts
git commit -m "feat: add TechCorp company documentation"
git push
```

Netlify otomatik deploy edecek! 🚀

---

## 📊 Özet Checklist

Yeni şirket eklerken kontrol listesi:

- [ ] `docs/companies/{şirket}/` klasörü oluşturuldu
- [ ] En az bir örnek doküman eklendi
- [ ] `merge-docs.js` → şirket listesine eklendi
- [ ] `sidebars.{şirket}.ts` dosyası oluşturuldu
- [ ] `docusaurus.config.ts` → plugins'e eklendi
- [ ] `npm run merge` çalıştırıldı
- [ ] `npm start` ile test edildi
- [ ] URL'ler çalışıyor: `/{şirket}/docs/intro`
- [ ] Git'e commit edildi
- [ ] Netlify'da deploy oldu

---

## 🎯 Örnekler

### Örnek 1: Sadece Yeni Kategori

TechCorp için sadece özel bir kategori:

```
docs/companies/techcorp/
└── techcorp-special/
    ├── intro.md
    ├── feature-1.md
    └── feature-2.md
```

**Sonuç:** `/techcorp/docs/` → Ortak + TechCorp Special kategorisi

### Örnek 2: Mevcut Kategoriye Ekleme

TechCorp için Octo Web'e özel dosyalar:

```
docs/companies/techcorp/
└── octo-web/
    ├── techcorp-api.md
    └── techcorp-integration.md
```

**Sonuç:** `/techcorp/docs/octo-web/` → Ortak Octo Web + TechCorp özel dosyalar

### Örnek 3: Override (Dosya Değiştirme)

TechCorp için farklı bir "yetkilendirme" sayfası:

```
docs/companies/techcorp/
└── octo-web/
    └── Yönetim/
        └── yetkilendirme.md  # Ortak dosyanın üzerine yazar
```

**Sonuç:** `/techcorp/docs/octo-web/Yönetim/yetkilendirme` → TechCorp versiyonu gösterilir

---

## 🔧 Dosya Yönetimi

### Ortak Dosya Eklemek

Tüm şirketler görecek:

```bash
# Herkes için yeni dosya
nano docs/common/octo-web/yeni-feature.md
```

### Şirkete Özel Dosya Eklemek

Sadece TechCorp görecek:

```bash
# Sadece TechCorp için
nano docs/companies/techcorp/octo-web/techcorp-feature.md
```

### Dosya Kaldırmak

```bash
# Ortak dosyayı kaldır (herkesten)
rm docs/common/octo-web/eski-dosya.md

# TechCorp'tan kaldır
rm docs/companies/techcorp/octo-web/eski-dosya.md

# Merge ve test
npm run merge
npm start
```

---

## 📁 Klasör Yapısı Şeması

```
renium-docs/
├── docs/
│   ├── common/                      # Ortak dokümanlar
│   │   ├── intro.md
│   │   ├── octo-desktop/
│   │   ├── octo-web/
│   │   └── backup/
│   │
│   ├── companies/                   # Şirkete özel
│   │   ├── renium/
│   │   │   └── renium-special/
│   │   ├── aws/
│   │   │   └── aws-special/
│   │   ├── azure/
│   │   │   └── azure-special/
│   │   └── techcorp/                # YENİ ŞİRKET
│   │       └── techcorp-special/
│   │
│   └── merged/                      # Otomatik oluşur
│       ├── public/
│       ├── renium/
│       ├── aws/
│       ├── azure/
│       └── techcorp/                # YENİ ŞİRKET
│
├── merge-docs.js                    # Güncelle
├── docusaurus.config.ts             # Güncelle
├── sidebars.ts
├── sidebars.renium.ts
├── sidebars.aws.ts
├── sidebars.azure.ts
└── sidebars.techcorp.ts             # YENİ DOSYA
```

---

## 🌐 URL Yapısı

Yeni şirket eklendikten sonra URL'ler:

```
Public (herkes):
https://renium-docs.netlify.app/docs/octo-web/

Renium:
https://renium-docs.netlify.app/renium/docs/octo-web/

AWS:
https://renium-docs.netlify.app/aws/docs/octo-web/

Azure:
https://renium-docs.netlify.app/azure/docs/octo-web/

TechCorp (YENİ):
https://renium-docs.netlify.app/techcorp/docs/intro
https://renium-docs.netlify.app/techcorp/docs/octo-web/
https://renium-docs.netlify.app/techcorp/docs/techcorp-special/
```

---

## 🚨 Sorun Giderme

### "No docs found" Hatası

```bash
# Klasör ve dosyaların var olduğunu kontrol edin
ls -la docs/companies/techcorp/

# Merge'i yeniden yapın
rm -rf docs/merged
npm run merge
```

### "Sidebar category has no items" Hatası

Sidebar'da olmayan bir klasör referans etmişsiniz:

```typescript
// YANLIŞ - klasör yok
{
  type: 'autogenerated',
  dirName: 'olmayan-klasor',  // ❌
}

// DOĞRU - klasör var
{
  type: 'autogenerated',
  dirName: 'techcorp-special',  // ✅
}
```

### Plugin ID Çakışması

Her şirketin benzersiz bir ID'si olmalı:

```typescript
// YANLIŞ - ID'ler aynı
{ id: 'techcorp' }
{ id: 'techcorp' }  // ❌ Çakışma!

// DOĞRU - Benzersiz ID'ler
{ id: 'techcorp' }
{ id: 'newcompany' }  // ✅
```

### Merge Sonrası Dosyalar Görünmüyor

```bash
# 1. Node cache'i temizle
rm -rf node_modules/.cache

# 2. Merged'i temizle
rm -rf docs/merged

# 3. Yeniden merge
npm run merge

# 4. Start
npm start
```

---

## 💡 İpuçları

### 1. Şirket Adı Formatı

- Klasör adı: **küçük harf, tire ile** → `techcorp`, `my-company`
- Sidebar ID: **küçük harf, tire ile** → `techcorp`, `mycompany`
- Plugin ID: **küçük harf, tire ile** → `'techcorp'`, `'my-company'`

### 2. Sidebar Kategorileri

Sadece **mevcut olan** klasörleri sidebar'a ekleyin:

```typescript
// Önce klasörü oluşturun
mkdir docs/companies/techcorp/demo

// Sonra sidebar'a ekleyin
{
  type: 'category',
  label: 'Demo',
  items: [
    { type: 'autogenerated', dirName: 'demo' }
  ]
}
```

### 3. Test Stratejisi

Her zaman şu sırayla test edin:

```bash
# 1. Local merge
npm run merge

# 2. Klasörleri kontrol et
ls -la docs/merged/techcorp/

# 3. Local test
npm start

# 4. URL'leri kontrol et
http://localhost:3000/techcorp/docs/intro

# 5. Git push (emin olduktan sonra)
git push
```

### 4. Netlify Deploy

Netlify'da otomatik çalışacak:

```
Git Push → Netlify
    ↓
npm install
    ↓
npm run build
    ↓
prebuild: npm run merge  ← Otomatik çalışır!
    ↓
Dokümanlar birleşir
    ↓
Build tamamlanır
    ↓
Deploy ✅
```

---

## 📚 Hızlı Referans Tablosu

| Ne Yapıyorsunuz? | Dosya/Komut |
|------------------|-------------|
| Yeni şirket klasörü | `mkdir -p docs/companies/{şirket}/` |
| Şirket listesine ekle | `merge-docs.js` → `companies` array |
| Sidebar oluştur | `sidebars.{şirket}.ts` |
| Config'e ekle | `docusaurus.config.ts` → `plugins` |
| Dokümanları birleştir | `npm run merge` |
| Local test | `npm start` |
| Deploy | `git push` |

---

## 🎉 Tamamlandı!

Artık yeni şirket dokümantasyonu hazır! 

- ✅ Şirket URL'si: `/{şirket}/docs/`
- ✅ Ortak + özel dokümanlar birleşik
- ✅ Sadece URL'yi bilenler erişebilir
- ✅ Netlify otomatik deploy ediyor

---

**Sorularınız için:** Bu dosyayı referans olarak kullanın veya geliştirme ekibine danışın.

**Versiyon:** 1.0  
**Son Güncelleme:** 11 Kasım 2025